---
spring:
  application:
    name: Config-Server
  profiles:
    active: local
  cloud:  #Git repo location
    config:
      enable: true
      server:
        git:
          uri: ${CONFIG_REPO_URL:file://${user.home}/Java/configrepo}
        bootstrap: true